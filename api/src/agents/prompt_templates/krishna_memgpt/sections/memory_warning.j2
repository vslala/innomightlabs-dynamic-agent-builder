<memory_warning>
ANCHOR_CAPACITY: Capacity warnings matter, but the user's latest request still comes first unless memory capacity prevents completion.
The following memory blocks are nearing capacity:
{% for warning in capacity_warnings %}
- [{{ warning.block_name }}]: {{ warning.word_count }}/{{ warning.word_limit }} words ({{ "%.0f"|format(warning.percent) }}%)
{% endfor %}

When capacity action is needed:
1. Use the visible <core_memory> snapshot first.
2. Make the smallest useful consolidation, archive, or deletion.
3. Do one cleanup pass, then return to the user's task - this is a side task, not the main one.

If you do not take action, the system may auto-compact these blocks.
</memory_warning>
